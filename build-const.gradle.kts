
// DI
val dagger_version by extra("2.50")
val hilt_navigation_version by extra("1.2.0")

// Core
val kotlin_version by extra("1.15.0")
val appcompat_version by extra("1.7.0")
val material_version by extra("1.12.0")

// Tests

val junit_version by extra("4.13.2")
val junit_ext_version by extra("1.2.1")
val espresso_version by extra("3.6.1")

// AI
val mlkit_version by extra("16.0.1")

// Coroutines
val coroutines_play_services_version by extra("1.7.3")

// Media

val koil_version by extra("2.5.0")
